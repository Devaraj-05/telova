import { WorkspacePlaceholderPage } from "@/components/workspace/WorkspacePlaceholderPage";

export default function ReplansPage() {
  return (
    <WorkspacePlaceholderPage
      activeItem="replans"
      title="Replans queue is ready"
      description="Use the workspace to trigger proposal revisions now. This route is ready for a dedicated replans history view."
    />
  );
}
