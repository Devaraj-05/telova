import { WorkspacePlaceholderPage } from "@/components/workspace/WorkspacePlaceholderPage";

export default function SettingsPage() {
  return (
    <WorkspacePlaceholderPage
      activeItem="settings"
      title="Settings route is ready"
      description="Workspace interactions now stay focused in chat while this route is reserved for account, integrations, and environment controls."
    />
  );
}
