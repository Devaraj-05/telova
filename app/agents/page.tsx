import { WorkspacePlaceholderPage } from "@/components/workspace/WorkspacePlaceholderPage";

export default function AgentsPage() {
  return (
    <WorkspacePlaceholderPage
      activeItem="agents"
      title="Agent diagnostics route is ready"
      description="The right insight rail already shows live agent work. This route is kept ready for a future dedicated diagnostics page."
    />
  );
}
