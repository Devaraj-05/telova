import { WorkspacePlaceholderPage } from "@/components/workspace/WorkspacePlaceholderPage";

export default function NotesPage() {
  return (
    <WorkspacePlaceholderPage
      activeItem="notes"
      title="Notes route is ready"
      description="Notes and memory stay visible in the workspace today, and this route is ready for the dedicated notes surface in the next pass."
    />
  );
}
